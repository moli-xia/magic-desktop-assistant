# 魔力桌面助手 - 跨平台依赖配置
# 支持 Windows, Linux, 麒麟Linux, macOS

# 核心GUI框架
ttkbootstrap>=1.10.1
Pillow>=9.0.0

# 网络请求
requests>=2.25.0

# 系统托盘支持
pystray>=0.19.0

# 任务调度
schedule>=1.2.0

# 跨平台兼容依赖
psutil>=5.8.0

# Windows特定依赖 (在Windows上自动安装)
pywin32>=227; sys_platform == "win32"
pywin32-ctypes>=0.2.0; sys_platform == "win32"

# macOS特定依赖 (在macOS上自动安装)
pyobjc-core>=8.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=8.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=8.0; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=8.0; sys_platform == "darwin"

# Linux特定依赖 (在Linux上自动安装)
# 注意：某些系统包需要通过系统包管理器安装
# Ubuntu/Debian: sudo apt-get install python3-tk libgtk-3-0
# CentOS/RHEL: sudo yum install tkinter gtk3-devel
# 麒麟系统: 参考对应构建脚本的说明